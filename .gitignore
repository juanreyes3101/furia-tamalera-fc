# NUNCA subir el roster con datos personales del equipo (cédulas, celulares,
# correos, fechas de nacimiento). Este archivo se queda solo en tu computador.
roster_interno_CONFIDENCIAL.csv
roster_interno*.csv

# Nombres legales completos de los 16 (3 son menores de edad) usados como
# clave interna para el sync — se queda solo en tu computador. Lo único que
# sale de acá hacia el repo público es data/players.json, con lo que tú ya
# decidiste publicar (nombre_publico/apellido/apodo/edad).
data/roster_publico_TEMPLATE.csv

# Respuestas reales del form de Tally (pueden llegar a tener datos sensibles
# según lo que la gente escriba en las preguntas abiertas)
data/respuestas_form.csv
data/ajustes.csv
data/tally_export.json

# Basura habitual de macOS / editores
.DS_Store
*.pyc
__pycache__/
.vscode/

# --- Next.js ---
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
/coverage
/.next/
/out/
/build
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.env*
.vercel
*.tsbuildinfo
next-env.d.ts
